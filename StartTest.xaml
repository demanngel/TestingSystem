<Window x:Class="TestingSystem.StartTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestingSystem"
        mc:Ignorable="d"
        Title="" Height="300" Width="500">
    <Grid Style="{StaticResource GridStyle}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <Border Style="{StaticResource BorderStyle}" Height="200" Width="300" Grid.RowSpan="2" VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                    <Label Style="{StaticResource TextStyle}" x:Name="LBTitle" HorizontalAlignment="Center" Margin="0,10,0,10"></Label>
                    <Label Style="{StaticResource TextStyle}" x:Name="LBInfo" HorizontalAlignment="Center" FontSize="18"></Label>
             </StackPanel>
        </Border>
        <Button Style="{StaticResource ButtonStyle}" Height="30" Width="80" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Начать" Click="Button_Click" Margin="0,10,0,0"></Button>

        </StackPanel>
        
    </Grid>
</Window>
